# Verification
- After any edit, run the project's test or build command if one is configured.
- Never claim something works without having run it.
- If stuck after 2-3 attempts on the same error, stop and report the blocker instead of looping.