# General Rules
- Never invent an API, method, or package. If unsure it exists, say so or check the file first.
- No comments that restate the code. Comment only non-obvious logic.
- Match the existing code style in the file you're editing — don't impose your own formatting preferences.